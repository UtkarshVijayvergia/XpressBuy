#! /bin/bash
# Command: ./bin/frontend/build-image

# This script is used to build the frontend image


set -e 
# -e : to exit the script if any command fails


# Load environment variables from .env file
# export $(cat .env | xargs)


# Build the frontend docker image
docker build \
--build-arg REACT_APP_BACKEND_URL="http://xpressbuy-backend-alb-262308006.us-east-1.elb.amazonaws.com" \
--build-arg REACT_APP_AWS_PROJECT_REGION="$REACT_APP_AWS_PROJECT_REGION" \
--build-arg REACT_APP_AWS_COGNITO_REGION="$REACT_APP_AWS_COGNITO_REGION" \
--build-arg REACT_APP_AWS_USER_POOLS_ID="us-east-1_QeC5UeKmH" \
--build-arg REACT_APP_AWS_USER_POOL_WEB_CLIENT_ID="33mobrtgt4afr6qobfn4dvg6m" \
-t frontend-xpressbuy \
-f ./frontend/Dockerfile.prod \
./frontend